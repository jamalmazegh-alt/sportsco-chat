# Project Memory

## Core
Linter SECURITY DEFINER (0028/0029): warnings restants sur helpers RLS, RPC utilisateur et RPC anon (respond_via_token, get_convocation_by_token, email_exists, get_tournament_invite_by_token, get_member_invite_info, user_can_email_recipient) sont acceptés — chaque fonction est protégée par token ou contrôle interne. Ne pas tenter de les "corriger" en révoquant authenticated/anon, ça casse RLS et flux email.
Endpoint /lovable/email/transactional/send durci: allowlist de 9 templates user (player-invite, convocation-invite/cancelled/response, event-cancelled/rescheduled, tournament-invite, tournament-member-added, absence-declared), rate limit 10/min/user via public_rate_limits (bucket minute, ip=user_id), scope check via RPC user_can_email_recipient. Tout autre template ⇒ envoi serveur via enqueueTransactionalEmailServer uniquement.
Realtime: app utilise uniquement postgres_changes (RLS sur tables source). Pas de canaux Broadcast/Presence private. realtime.messages sans policy = OK.
Storage public buckets (player-photos, club-logos, team-images, attachments, tournament-documents): pas de SELECT policy → pas de listing, accès uniquement via URL CDN directe. Ne pas ajouter de policy de listing.
Branding par club: `clubs.theme_color` (palette 10 clés dans src/lib/club-themes.ts) appliqué via ClubThemeProvider (CSS vars sur :root). Persisté dans localStorage `clubero:active_theme` pour skinner la page de login. Page admin: /admin/settings/branding.
Clubs personnels: handle_new_user ne crée PAS de club perso à l'inscription. Les clubs `is_personal=true` proviennent uniquement du flow "orga libre" (création tournoi sans club). Superadmin filtre par défaut `is_personal=false` et exclut les `__rls_*`/`__e2e_*`. RPC `convert_personal_club_to_real(_club_id, _new_name)` pour transformer une orga libre en vrai club.
Postes de paiement (anciennement "items de paiement", terme français interdit côté UI): wording UI = "poste(s) de paiement". À la création d'un poste `status=open`, `createPaymentItem` envoie automatiquement un email `payment-reminder` (avec `kind:"initial"` → titre "Nouveau paiement à régler") au payeur (tuteur principal), au joueur et aux parents — idempotent par (obligation_id × email). Lecture/relance accessibles aux rôles `admin` ET `financial_admin` (assertAdminOrFinAdmin / assertAdminOrFinancialAdmin). Mutations financières (encaissement manuel, refund, exempt, cancel) restent réservées à `financial_admin` strict. Doc complète dans le header de src/lib/payment-items.functions.ts.
Funnel conversion tournoi→club: composant `TournamentToClubBanner` (src/components/tournament-to-club-banner.tsx) inséré dans page admin tournoi quand >=1 résultat saisi OU status=completed, non-dismissé (localStorage TTL 30j), user sans club. CTA → /pricing?source=tournament_conversion&tournament_id=X. Table `conversion_events` (anon/auth INSERT, superadmin SELECT) loggue banner_seen/banner_clicked/pricing_viewed/trial_started/club_activated/payment_completed via `trackConversion()` (src/lib/conversion-tracking.ts). Email `tournament-completed` envoyé automatiquement à l'organisateur quand `updateTournament` passe `status` à completed (idempotency `tournament-completed:<id>`). i18n `tournamentToClub.*` + `pricing.tournamentHero/clubBenefits` dans les 7 locales.

