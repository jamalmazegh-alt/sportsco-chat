<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug uniquement (src/debug) : jamais fusionné dans un build de release.

  Android bloque le trafic en clair depuis l'API 28. Le serveur de développement
  (`bun run dev:qa` / `vite preview`) est servi en HTTP sur la machine hôte, que
  l'émulateur atteint via l'alias 10.0.2.2.

  Autorisation volontairement restreinte à ces deux hôtes : tout le reste, y
  compris en debug, reste soumis au blocage du clair.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
